"use client"
function Dashboard() {

    // const { status } = useSession({
    //     required: true,
    //     onUnauthenticated() {
    //         redirect("/login");
    //     },
    // });

    // if (status === "loading") {
    //     return <p>Loading....</p>;
    // }

    return (
        <div>
            <h1>This is Dashboard</h1>
        </div>
    )
}

export default Dashboard;